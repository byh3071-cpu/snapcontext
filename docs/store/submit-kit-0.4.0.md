# v0.4.0 제출 킷 — 복붙 전용 (2026-07-23)

> **재제출 대상**: 0.4.0 은 확장 코드(`src/**`)가 변경됐다(온보딩 토큰 UI 신규) — 문서만
> 바뀐 게 아니라 **크롬·웨일 모두 새 패키지 업로드 + 재심사**가 필요하다. 0.3.0(문서 델타만)과
> 다르다. 실제 제출은 사람 게이트.
>
> zip: `C:\Users\Public\dev\products\snapcontext\snapcontext-v0.4.0.zip` (배포 직전 빌드)
> 이미지: 스크린샷 04(공유)의 "7일" 카피가 "보관 기간 선택"으로 바뀌므로 **04 스크린샷만
> 재생성** 후 교체(`node scripts/generate-store-screenshots.mjs`). 나머지는 유지.

## 0. 배포 선행 조건 (이 킷 제출 전에 사람 게이트로 완료돼야 함)

| # | 항목 | 이유 |
|---|------|------|
| 1 | R2 lifecycle `auto-delete-7d` → 30일 상향 | 30일 캡처가 7일에 물리 삭제되는 모순 방지(ADR-013) |
| 2 | `wrangler secret put TOKEN_SIGNING_SECRET` | 미주입이면 `/token` 500 → 전원 익명(owner 격리 미동작) |
| 3 | `wrangler deploy` (worker 0.4.0) | /token·owner·만료 파라미터화 라이브 |
| 4 | Origin 검증 실 워커 스모크 | `POST /token` 이 `chrome-extension://` 아니면 403 확인(e2e 가 mock 해 자동 테스트에 없음) |
| 5 | 0.4.0 zip 빌드 (`pnpm build` → dist zip) | manifest 0.4.0 반영 |

---

## 1. Chrome Web Store — https://chrome.google.com/webstore/devconsole

1. SnapContext 항목 클릭 → 좌측 "패키지" → **새 패키지 업로드** → 0.4.0 zip 선택
2. 좌측 "스토어 등록정보" → 아래 두 칸 교체:

**짧은 설명 (그대로 복붙):**

```
캡처 한 번이면 AI에 붙여넣을 컨텍스트(프롬프트)가 자동 완성됩니다. 영역·요소·문서·풀페이지 캡처, 보관 기간을 고르는 익명 공유, Claude Code·Cursor 연동까지.
```

**상세 설명 (전체 교체 — 그대로 복붙):**

```
화면을 캡처하는 순간, AI에 그대로 붙여넣을 컨텍스트(프롬프트)가 완성됩니다.
아이콘을 클릭하거나 Alt+Shift+V를 누르면 영역 캡처가 시작되고, 캡처와 동시에
화면·핀 주석·출처 정보가 프롬프트로 정리됩니다. 설명을 다시 쓸 필요가 없습니다.

✨ 이렇게 동작합니다
1️⃣ 캡처 — 영역·요소·문서·풀페이지 중 원하는 방식으로
2️⃣ 컨텍스트 생성 — 캡처 + 핀 주석 + 출처 메타데이터가 Context Pack(프롬프트)으로 자동 완성
3️⃣ 전달 — ChatGPT·Claude에 붙여넣거나, 익명 링크로 공유하거나, Claude Code·Cursor가 직접 조회

🔗 AI 코딩 도구 연동 — v0.4.0 강화
공유한 캡처를 Claude Code·Cursor가 직접 불러올 수 있습니다(MCP).
설정 패널에서 개인 토큰을 복사해 붙여넣기만 하면 연결이 끝납니다. 연결 뒤에는
"방금 공유한 버그 화면 보고 고쳐줘"라고 말하면 에이전트가 내 캡처 목록을 조회하고
핀 메모·출처 정보까지 함께 가져갑니다. 복사·붙여넣기 단계가 사라집니다.
개인 토큰으로 격리되어 내가 올린 캡처만 조회됩니다. 여러 기기를 쓰면 토큰을
붙여넣어 하나로 합칠 수 있습니다.

🔒 프라이버시 먼저
캡처는 기기 안에서 처리됩니다. 공유 링크를 만들 때만 업로드되며, 직접 고른 보관
기간(1·7·30일, 기본 7일)이 지나면 접근이 차단되고 서버에서도 자동 영구 삭제됩니다.
계정도, 로그인도, 추적도 없습니다. 개인 토큰은 신원이 아니라 내 캡처를 구분하는
익명 식별자이며 서버에 저장되지 않습니다. '컨텍스트 포함'은 기본 꺼짐 + 최초 1회
동의제 — 토큰·쿠키·로컬 데이터는 전송하지 않습니다. 추가 권한 요청 없음 ·
Manifest V3 · 누출 차단 E2E 검증.

💡 이럴 때 씁니다
· 버그 화면을 설명 없이 AI에게 그대로 던질 때
· 디자인 피드백을 화면 컨텍스트째 익명 링크로 전달할 때
· 긴 문서·페이지를 출처와 함께 한 장으로 남길 때

📸 캡처 4모드 + AI 컨텍스트
· 영역 선택 / 요소 선택 / 문서 본문 / 풀페이지 스크롤 전체
· 캡처 위에 핀을 찍고 메모하면 주석까지 프롬프트에 포함됩니다
· 버그 리포트 · 리팩토링 · 레퍼런스 템플릿 제공

⌨️ 단축키 (기본 4개)
Alt+Shift+V — 영역 캡처
Alt+Shift+E — 요소 캡처
Alt+Shift+M — 문서 본문 캡처
Alt+Shift+G — 풀페이지 캡처
PNG 복사는 브라우저 단축키 설정에서 직접 지정할 수 있습니다.

💬 의견을 들려주세요
문제가 있거나 바라는 기능이 있다면 리뷰보다 먼저 알려주세요. 다음 업데이트에
반영하겠습니다. → github.com/byh3071-cpu/SnapContext/issues
```

3. Privacy practices 탭: **권한 변화 없음** (permissions·host_permissions 0.3.0과 동일, 신규 권한 0). 데이터 흐름: 개인 토큰(로컬 저장, 서버 미저장) 추가되나 신원 미수집은 불변 — PRIVACY.md 반영본 확인
4. 우측 상단 **심사를 위해 제출**

## 2. 네이버 웨일 스토어 — 개발자 센터

1. SnapContext 항목 → 새 버전 zip 업로드 (0.4.0)
2. 설명 교체 (**평문판 — 그대로 복붙**):

```
화면을 캡처하는 순간, AI에 그대로 붙여넣을 컨텍스트(프롬프트)가 완성됩니다.
아이콘을 클릭하거나 Alt+Shift+V를 누르면 영역 캡처가 시작되고, 캡처와 동시에
화면·핀 주석·출처 정보가 프롬프트로 정리됩니다. 설명을 다시 쓸 필요가 없습니다.

[이렇게 동작합니다]
1. 캡처 — 영역·요소·문서·풀페이지 중 원하는 방식으로
2. 컨텍스트 생성 — 캡처 + 핀 주석 + 출처 메타데이터가 Context Pack(프롬프트)으로 자동 완성
3. 전달 — ChatGPT·Claude에 붙여넣거나, 익명 링크로 공유하거나, Claude Code·Cursor가 직접 조회

[AI 코딩 도구 연동 — v0.4.0 강화]
공유한 캡처를 Claude Code·Cursor가 직접 불러올 수 있습니다(MCP).
설정 패널에서 개인 토큰을 복사해 붙여넣기만 하면 연결이 끝납니다. 연결 뒤에는
"방금 공유한 버그 화면 보고 고쳐줘"라고 말하면 에이전트가 내 캡처 목록을 조회하고
핀 메모·출처 정보까지 함께 가져갑니다. 복사·붙여넣기 단계가 사라집니다.
개인 토큰으로 격리되어 내가 올린 캡처만 조회됩니다. 여러 기기를 쓰면 토큰을
붙여넣어 하나로 합칠 수 있습니다.

[프라이버시 먼저]
캡처는 기기 안에서 처리됩니다. 공유 링크를 만들 때만 업로드되며, 직접 고른 보관
기간(1·7·30일, 기본 7일)이 지나면 접근이 차단되고 서버에서도 자동 영구 삭제됩니다.
계정도, 로그인도, 추적도 없습니다. 개인 토큰은 신원이 아니라 내 캡처를 구분하는
익명 식별자이며 서버에 저장되지 않습니다. '컨텍스트 포함'은 기본 꺼짐 + 최초 1회
동의제 — 토큰·쿠키·로컬 데이터는 전송하지 않습니다. 추가 권한 요청 없음 ·
Manifest V3 · 누출 차단 E2E 검증.

[이럴 때 씁니다]
· 버그 화면을 설명 없이 AI에게 그대로 던질 때
· 디자인 피드백을 화면 컨텍스트째 익명 링크로 전달할 때
· 긴 문서·페이지를 출처와 함께 한 장으로 남길 때

[캡처 4모드 + AI 컨텍스트]
· 영역 선택 / 요소 선택 / 문서 본문 / 풀페이지 스크롤 전체
· 캡처 위에 핀을 찍고 메모하면 주석까지 프롬프트에 포함됩니다
· 버그 리포트 · 리팩토링 · 레퍼런스 템플릿 제공

[단축키 (기본 4개)]
Alt+Shift+V — 영역 캡처
Alt+Shift+E — 요소 캡처
Alt+Shift+M — 문서 본문 캡처
Alt+Shift+G — 풀페이지 캡처
PNG 복사는 브라우저 단축키 설정(whale://extensions/shortcuts)에서 직접 지정할 수 있습니다.

[의견을 들려주세요]
문제가 있거나 바라는 기능이 있다면 리뷰보다 먼저 알려주세요. 다음 업데이트에
반영하겠습니다. → github.com/byh3071-cpu/SnapContext/issues
```

3. 스크린샷: 04(공유)만 보관 기간 카피로 재생성 교체, 나머지 유지 · 분류 생산성 · 성인콘텐츠 아니요 → 제출

## 3. 심사 메모 (물어보면)

```
v0.4.0 — 공유 보관 기간 선택(1/7/30일) + AI 도구 연동 온보딩 UI(설정 패널에서 개인
토큰 복사·붙여넣기) + 사용자별 조회 격리. 확장 권한 변화 없음(설정 UI 추가만).
```
