---
id: log-2026-07-18-v030
date: 2026-07-18
tags: [log, v0.3.0, mcp, release]
---

# 2026-07-18 — v0.3.0 원격 MCP 서버 풀사이클 (Phase 0~4)

## 결과

- **v0.3.0 릴리즈**: Worker 원격 MCP 서버(`/mcp`) + 툴 3종 + D1 수집 파이프라인. 배포 버전 `4a91cc35`, 엔드포인트 `https://snapcontext-worker.byh3071-26a.workers.dev/mcp`.
- 확장(src/**) 코드 변경 0 — 스토어 재제출 불요(트랙 A 별도).
- PR #13(Phase 0+1) · #14(Phase 2) · #15(Phase 3) + 릴리즈 PR. 멀티벤더 풀개발 체인(claude 지휘·cursor 구현·codex/claude 적대 검증 4라운드).

## 배포본 E2E 스모크 (2026-07-18, DoD 1~3 실증)

```
N1 no-bearer POST /mcp -> 401 (기대 401)
N2 wrong-bearer -> 401 (기대 401)
N3 evil-Origin OPTIONS -> 403 (기대 403)
UP upload -> id=db08f141-a4cd-45ed-b1d5-5fddc3bd1617
M1 initialize -> serverInfo {snapcontext 0.3.0} 정상
M2 tools/list -> snap_history/snap_pack/snap_analyze True x3
M3 snap_history에 업로드 id 포함 -> True (수집 파이프라인 실동작)
M4 snap_pack -> sourceUrl 포함 True / isError False
M5 snap_analyze -> 핀 메모 포함 True / isError False
M6 없는 id snap_pack -> isError True (명시적 에러 — DoD 3. NOT_FOUND 라벨은 message 텍스트로 노출)
M7 mode allowlist 위반 -> isError True
```

재실행: `scripts/e2e-smoke.ps1` (bearer 토큰 = `worker/.secret-bearer-token.txt`, gitignored).

## 클라이언트 등록 (DoD 1~2 클라이언트 측 — 사용자 확인 항목)

```
claude mcp add --transport http snapcontext https://snapcontext-worker.byh3071-26a.workers.dev/mcp --header "Authorization: Bearer <worker/.secret-bearer-token.txt 값>"
```

Cursor: `~/.cursor/mcp.json`에 `{"mcpServers":{"snapcontext":{"url":"<위 URL>","headers":{"Authorization":"Bearer <토큰>"}}}}`

## 사람 게이트 실행 기록

wrangler 로그인(byh3071@gmail.com) → `d1 create snapcontext-captures`(4fb3b111, APAC) → migrations apply --remote(0001_captures ✅) → `secret put SNAPCONTEXT_BEARER_TOKEN` ✅ → `wrangler deploy` ✅. backfill은 기존 R2 데이터 0건(7일 lifecycle)이라 실행 불요 — 신규 업로드는 수집 파이프라인이 자동 인덱싱.

## 남은 것 (0.4+ / 트랙 A)

- MINOR 백로그: analyze 누출-회귀 테스트, malformed context 로그, cleanup 실패 테스트, 실 D1 왕복 테스트, `/upload` bearer(토큰 UI 동반)
- 트랙 A: 스위스 UI 이식 → 스토어 제출 (resume: `.vhk/handoff-2026-06-07-pm.md`)
- 다이제스트 프롬프트 인젝션 워치 (다중 사용자 전환 시 재평가)
