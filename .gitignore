node_modules
dist
.env
.env.local
*.local
*.local.*
*.log
.DS_Store
coverage
.vite
.claude/

# Notion API batch payloads (scratch from prior dev-log injection)
.notion-*.json

# E2E test outputs (regenerated each run)
tests/e2e/screenshots/

# Store upload artifacts (regenerate with npm run build)
snapcontext-*.zip


# vhk init
.env.*.local
node_modules/
dist/
*.tsbuildinfo

# Serena 툴 캐시 (.claude/ 와 동일 성격 — 추적 제외)
.serena/

# vhk 로컬 백업본 (context.md.local-bak 등)
*.local-bak

# 내부 디자인 탐색 (타사 브랜드 레퍼런스·실명 포함 — 공개 배포 제외, 로컬 보존)
docs/ui-audit/hermes/

# vhk 세션 핸드오프 (로컬 절대경로·내부 맥락 포함 — 공개 제외, 로컬 보존)
.vhk/handoff-*.md

# swiss 외부 디자인 레퍼런스 이미지 (타사 캡처 — 공개 배포 제외, 로컬 보존)
docs/ui-audit/swiss/refs/

# 로컬 시크릿 (커밋 절대 금지)
.secret-*.txt
.dev.vars

# Phase 4 1회용 로컬 스크립트 잔재 (정식본은 scripts/e2e-smoke.ps1)
worker/release-gates.ps1
worker/e2e-smoke.ps1
worker/e2e-results.txt
